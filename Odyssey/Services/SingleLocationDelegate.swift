// Deprecated — folded into LocationCaptureService.SingleLocationRequest.
// File kept so Xcode project references remain valid; no external callers.
import CoreLocation

@available(*, deprecated, message: "Use LocationCaptureService instead")
final class SingleLocationDelegate: NSObject, CLLocationManagerDelegate {}
